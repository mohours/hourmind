# Agents — SOP 操作手册

## 代码规范
- 每行代码必须有中文注释
- 优先使用原生方案，不过度引入框架
- 先出设计文档，用户确认后再实现
- 复杂任务用子代理并行处理

## 安全边界
- rm -rf / git push --force 需确认
- 不修改 .env / credentials 需确认
- 不在循环中操作数据库
- 禁止主动操作数据库
- 未明确指令禁止 git add / git commit / git push

## 报错处理
- 先诊断根因，再修复
- 不绕过校验
