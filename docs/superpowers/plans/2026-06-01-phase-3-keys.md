# HourMind 阶段 3 — API Key 管理 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现完整的 API Key CRUD 管理系统，包括 AES-256-GCM 加密存储、连通性测试、前端玻璃卡片展示。

**Prerequisites:** Phase 1（项目骨架）和 Phase 2（JWT 认证 + `auth.py` 含 `require_auth` dependency + `create_token`/`verify_token`）已完成。

**Architecture:** 后端：Python FastAPI REST 端点（`/api/keys/*`、`/api/providers`）+ AES-256-GCM 加密服务 + sqlite3 直连。前端：Pinia keyStore + KeysView 玻璃卡片列表 + AddKeyDialog 模态弹窗。

---

### Task 1: 创建 AES-256-GCM 加密服务

**Files:**
- Create: `server/services/__init__.py`
- Create: `server/services/crypto_service.py`

- [ ] **Step 1: 创建 `services/__init__.py`**

```bash
mkdir -p server/services
touch server/services/__init__.py
```

- [ ] **Step 2: 创建 `crypto_service.py`**

```python
# server/services/crypto_service.py
# AES-256-GCM 加密解密服务 —— API Key 安全存储核心
import os
import hashlib
from cryptography.hazmat.primitives.ciphers.aead import AESGCM
from config import ENCRYPTION_KEY


def _get_key() -> bytes:
    """将 ENCRYPTION_KEY 用 SHA-256 派生为 32 字节 AES 密钥"""
    return hashlib.sha256(ENCRYPTION_KEY.encode()).digest()


def encrypt(plaintext: str) -> str:
    """
    用 AES-256-GCM 加密明文
    返回格式: "nonce_hex:auth_tag_hex:ciphertext_hex"
    GCM 模式自动生成 auth_tag（16 字节），无需手动处理
    """
    key = _get_key()
    nonce = os.urandom(12)  # GCM 推荐 12 字节 nonce
    aesgcm = AESGCM(key)
    # AESGCM.encrypt 返回 ciphertext + 16 字节 auth_tag（已拼接在末尾）
    ciphertext_with_tag = aesgcm.encrypt(nonce, plaintext.encode(), None)
    # 分离 ciphertext 和 auth_tag（最后 16 字节是 tag）
    ciphertext = ciphertext_with_tag[:-16]
    auth_tag = ciphertext_with_tag[-16:]
    return f"{nonce.hex()}:{auth_tag.hex()}:{ciphertext.hex()}"


def decrypt(encrypted: str) -> str:
    """
    解密 "nonce_hex:auth_tag_hex:ciphertext_hex" 格式的密文
    返回明文
    """
    key = _get_key()
    nonce_hex, auth_tag_hex, ciphertext_hex = encrypted.split(":")
    nonce = bytes.fromhex(nonce_hex)
    auth_tag = bytes.fromhex(auth_tag_hex)
    ciphertext = bytes.fromhex(ciphertext_hex)
    aesgcm = AESGCM(key)
    # 重新拼接 ciphertext + auth_tag
    plaintext = aesgcm.decrypt(nonce, ciphertext + auth_tag, None)
    return plaintext.decode()
```

- [ ] **Step 3: 验证加密解密**

```bash
cd server
source venv/bin/activate
python -c "
from services.crypto_service import encrypt, decrypt
enc = encrypt('sk-test-key-12345')
print('Encrypted:', enc)
dec = decrypt(enc)
print('Decrypted:', dec)
assert dec == 'sk-test-key-12345', 'FAIL'
print('PASS')
"
```

预期输出：`PASS`

- [ ] **Step 4: 提交**

```bash
git add server/services/__init__.py server/services/crypto_service.py
git commit -m "feat: AES-256-GCM 加密解密服务 —— API Key 安全存储基础"
```

---

### Task 2: 添加 Key 相关 Pydantic 模型

**Files:**
- Create: `server/models.py`（如果 Phase 2 未建则新建，否则追加）

- [ ] **Step 1: 创建 `models.py` 中的 Key 模型**

```python
# server/models.py
# Pydantic 数据模型 —— 请求/响应校验
from pydantic import BaseModel
from typing import Optional


# ── Phase 2 已有的模型（auth） ──
# ...（Phase 2 中定义，此处省略）


# ── API Key 模型 ──

class KeyCreate(BaseModel):
    """创建 API Key 请求体"""
    provider_id: str          # 关联厂商 ID
    key_value: str            # 明文 Key（后端加密存储）
    alias: str = ""           # 用户自定义别名


class KeyResponse(BaseModel):
    """API Key 列表响应（不暴露 encrypted_key）"""
    id: str
    provider_name: str        # 联表查出的厂商名称
    alias: str
    key_suffix: str           # 后 6 位明文（前端展示）
    status: str               # active / disabled / deleted
    tags: str                 # JSON 数组字符串
    usage: str                # 用量 JSON 字符串
    test_result: str          # 测试历史 JSON 字符串
    created_at: Optional[str] = None
    updated_at: Optional[str] = None


class TestResult(BaseModel):
    """Key 连通性测试结果"""
    ok: bool
    latency_ms: int
    error: Optional[str] = None
    models: list[str] = []    # 返回可用模型列表
```

- [ ] **Step 2: 对 models.py 做语法检查**

```bash
cd server
source venv/bin/activate
python -c "from models import KeyCreate, KeyResponse, TestResult; print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add server/models.py
git commit -m "feat: 添加 API Key Pydantic 模型 —— KeyCreate / KeyResponse / TestResult"
```

---

### Task 3: 创建 Keys REST 路由

**Files:**
- Create: `server/routes/__init__.py`
- Create: `server/routes/keys.py`

- [ ] **Step 1: 创建 `routes/__init__.py`**

```bash
mkdir -p server/routes
touch server/routes/__init__.py
```

- [ ] **Step 2: 创建 `routes/keys.py`**

```python
# server/routes/keys.py
# API Key 管理 REST 端点 —— /api/providers 和 /api/keys/*
import json
import uuid
import time
from datetime import datetime, timezone

from fastapi import APIRouter, Depends, HTTPException
from database import get_db
from auth import require_auth
from services.crypto_service import encrypt, decrypt
from models import KeyCreate, KeyResponse, TestResult
import httpx

router = APIRouter()


# ── 厂商列表 ──

@router.get("/api/providers")
def list_providers(_token: str = Depends(require_auth)):
    """返回所有活跃的 AI 厂商"""
    db = get_db()
    rows = db.execute(
        "SELECT id, name, slug, base_url, logo_url FROM provider WHERE is_active = 1 ORDER BY name"
    ).fetchall()
    db.close()
    return [dict(row) for row in rows]


# ── Key 列表 ──

@router.get("/api/keys")
def list_keys(_token: str = Depends(require_auth)):
    """返回所有非删除状态的 Key，联表带出 provider name"""
    db = get_db()
    rows = db.execute("""
        SELECT ak.id, p.name AS provider_name, ak.alias, ak.key_suffix,
               ak.status, ak.tags, ak.usage, ak.test_result,
               ak.created_at, ak.updated_at
        FROM api_key ak
        JOIN provider p ON p.id = ak.provider_id
        WHERE ak.status != 'deleted'
        ORDER BY ak.created_at DESC
    """).fetchall()
    db.close()
    return [dict(row) for row in rows]


# ── 创建 Key ──

@router.post("/api/keys", status_code=201)
def create_key(body: KeyCreate, _token: str = Depends(require_auth)):
    """加密存储新 Key，返回创建后的 Key 对象"""
    db = get_db()

    # 校验厂商是否存在
    provider = db.execute(
        "SELECT id, name FROM provider WHERE id = ?", (body.provider_id,)
    ).fetchone()
    if not provider:
        db.close()
        raise HTTPException(400, "厂商不存在")

    # AES-256-GCM 加密
    encrypted = encrypt(body.key_value)
    key_suffix = body.key_value[-6:]  # 取明文后 6 位
    key_id = str(uuid.uuid4())
    now = datetime.now(timezone.utc).isoformat()

    db.execute("""
        INSERT INTO api_key (id, provider_id, alias, encrypted_key, key_suffix, status, created_at, updated_at)
        VALUES (?, ?, ?, ?, ?, 'active', ?, ?)
    """, (key_id, body.provider_id, body.alias, encrypted, key_suffix, now, now))
    db.commit()

    # 返回新建的 Key
    row = db.execute("""
        SELECT ak.id, p.name AS provider_name, ak.alias, ak.key_suffix,
               ak.status, ak.tags, ak.usage, ak.test_result,
               ak.created_at, ak.updated_at
        FROM api_key ak
        JOIN provider p ON p.id = ak.provider_id
        WHERE ak.id = ?
    """, (key_id,)).fetchone()
    db.close()
    return dict(row)


# ── 删除 Key（软删除） ──

@router.delete("/api/keys/{key_id}")
def delete_key(key_id: str, _token: str = Depends(require_auth)):
    """软删除：将 status 设为 'deleted'"""
    db = get_db()
    now = datetime.now(timezone.utc).isoformat()
    cur = db.execute(
        "UPDATE api_key SET status = 'deleted', updated_at = ? WHERE id = ? AND status != 'deleted'",
        (now, key_id)
    )
    db.commit()
    if cur.rowcount == 0:
        db.close()
        raise HTTPException(404, "Key 不存在或已删除")
    db.close()
    return {"ok": True}


# ── 启用/禁用切换 ──

@router.put("/api/keys/{key_id}/toggle")
def toggle_key(key_id: str, _token: str = Depends(require_auth)):
    """在 active 和 disabled 之间切换"""
    db = get_db()

    row = db.execute(
        "SELECT status FROM api_key WHERE id = ? AND status != 'deleted'",
        (key_id,)
    ).fetchone()
    if not row:
        db.close()
        raise HTTPException(404, "Key 不存在或已删除")

    new_status = "disabled" if row["status"] == "active" else "active"
    now = datetime.now(timezone.utc).isoformat()
    db.execute(
        "UPDATE api_key SET status = ?, updated_at = ? WHERE id = ?",
        (new_status, now, key_id)
    )
    db.commit()
    db.close()
    return {"ok": True, "status": new_status}


# ── 连通性测试 ──

@router.post("/api/keys/{key_id}/test")
def test_key(key_id: str, _token: str = Depends(require_auth)):
    """解密 Key，调厂商 /models 接口验证连通性，记录测试结果"""
    db = get_db()

    row = db.execute("""
        SELECT ak.id, ak.encrypted_key, p.base_url
        FROM api_key ak
        JOIN provider p ON p.id = ak.provider_id
        WHERE ak.id = ? AND ak.status != 'deleted'
    """, (key_id,)).fetchone()
    if not row:
        db.close()
        raise HTTPException(404, "Key 不存在或已删除")

    # 解密 Key
    try:
        plain_key = decrypt(row["encrypted_key"])
    except Exception:
        record_test(db, key_id, False, 0, "解密失败")
        db.close()
        return {"ok": False, "latency_ms": 0, "error": "解密失败", "models": []}

    # 调厂商 /models
    base_url = row["base_url"].rstrip("/")
    test_url = f"{base_url}/models"
    models = []

    try:
        start = time.time()
        resp = httpx.get(
            test_url,
            headers={"Authorization": f"Bearer {plain_key}"},
            timeout=15.0
        )
        latency_ms = int((time.time() - start) * 1000)

        if resp.status_code == 401 or resp.status_code == 403:
            record_test(db, key_id, False, latency_ms, "认证失败，请检查 Key 是否正确")
            db.close()
            return {"ok": False, "latency_ms": latency_ms, "error": "认证失败，请检查 Key 是否正确", "models": []}

        if resp.status_code != 200:
            record_test(db, key_id, False, latency_ms, f"HTTP {resp.status_code}")
            db.close()
            return {"ok": False, "latency_ms": latency_ms, "error": f"HTTP {resp.status_code}", "models": []}

        data = resp.json()
        # 尝试从返回中提取模型 ID 列表
        model_list = data.get("data", data.get("models", []))
        if isinstance(model_list, list):
            models = [m.get("id", str(m)) if isinstance(m, dict) else str(m) for m in model_list[:50]]

        record_test(db, key_id, True, latency_ms, None)
        db.close()
        return {"ok": True, "latency_ms": latency_ms, "error": None, "models": models}

    except httpx.TimeoutException:
        record_test(db, key_id, False, 0, "请求超时（15s）")
        db.close()
        return {"ok": False, "latency_ms": 0, "error": "请求超时（15s）", "models": []}
    except Exception as e:
        record_test(db, key_id, False, 0, str(e))
        db.close()
        return {"ok": False, "latency_ms": 0, "error": str(e), "models": []}


def record_test(db, key_id: str, ok: bool, latency_ms: int, error: str | None):
    """将测试结果记录到 api_key.test_result JSON 字段，保留最近 7 次"""
    row = db.execute("SELECT test_result FROM api_key WHERE id = ?", (key_id,)).fetchone()
    records = json.loads(row["test_result"]) if row else []
    records.append({
        "time": datetime.now(timezone.utc).isoformat(),
        "success": ok,
        "latency_ms": latency_ms,
        "error": error,
    })
    # 只保留最近 7 次
    records = records[-7:]
    db.execute(
        "UPDATE api_key SET test_result = ?, updated_at = ? WHERE id = ?",
        (json.dumps(records, ensure_ascii=False), datetime.now(timezone.utc).isoformat(), key_id)
    )
    db.commit()
```

- [ ] **Step 3: 创建或更新 `server/auth.py` 中的 `require_auth` dependency**

如果 Phase 2 已正确实现 `auth.py`，跳过此步骤。否则创建 `auth.py`：

```python
# server/auth.py
# JWT 认证 —— token 签发、验证、FastAPI dependency
import bcrypt
from datetime import datetime, timedelta, timezone
from jose import jwt, JWTError
from fastapi import Header, HTTPException
from config import JWT_SECRET

ALGORITHM = "HS256"
EXPIRE_HOURS = 24


def hash_password(password: str) -> str:
    """bcrypt 哈希密码"""
    return bcrypt.hashpw(password.encode(), bcrypt.gensalt()).decode()


def verify_password(password: str, hashed: str) -> bool:
    """验证密码是否匹配哈希"""
    return bcrypt.checkpw(password.encode(), hashed.encode())


def create_token(payload: dict) -> str:
    """签发 JWT，默认 24h 有效"""
    to_encode = payload.copy()
    to_encode["exp"] = datetime.now(timezone.utc) + timedelta(hours=EXPIRE_HOURS)
    return jwt.encode(to_encode, JWT_SECRET, algorithm=ALGORITHM)


def verify_token(token: str) -> dict:
    """验证 JWT 并返回 payload，失败抛 JWTError"""
    return jwt.decode(token, JWT_SECRET, algorithms=[ALGORITHM])


def require_auth(authorization: str = Header(None)) -> str:
    """
    FastAPI dependency —— 从 Authorization header 解出 token 并验证
    验证通过返回 token，失败抛 401
    """
    if not authorization or not authorization.startswith("Bearer "):
        raise HTTPException(401, "Missing or invalid Authorization header")
    token = authorization[7:]  # "Bearer " 后 7 个字符
    try:
        verify_token(token)
    except JWTError:
        raise HTTPException(401, "Invalid or expired token")
    return token
```

- [ ] **Step 4: 提交**

```bash
git add server/routes/__init__.py server/routes/keys.py
git add server/auth.py  # 如果有新建
git commit -m "feat: API Key REST 路由 —— 列表/创建/删除/切换/测试 全端点"
```

---

### Task 4: 注册路由到 main.py

**Files:**
- Edit: `server/main.py`

- [ ] **Step 1: 在 `server/main.py` 中导入并注册 keys router**

```python
# ... 在已有 import 区块末尾追加
from routes.keys import router as keys_router

# ... 在 app = FastAPI(...) 之后、if __name__ 之前追加
app.include_router(keys_router)
```

- [ ] **Step 2: 验证后端启动无误**

```bash
cd server
source venv/bin/activate
python -c "from main import app; print('App loaded OK, routes:', [r.path for r in app.routes])"
```

应看到输出中包含 `/api/providers`、`/api/keys`、`/api/keys/{key_id}` 等路由路径。

- [ ] **Step 3: 提交**

```bash
git add server/main.py
git commit -m "feat: 注册 Keys 路由到 FastAPI main.py"
```

---

### Task 5: 创建 keyStore（Pinia）

**Files:**
- Create: `client/src/api/keys.ts`
- Create: `client/src/stores/keyStore.ts`

- [ ] **Step 1: 创建 API 请求封装 `client/src/api/keys.ts`**

```bash
mkdir -p client/src/api
```

```typescript
// client/src/api/keys.ts
// API Key 相关的 HTTP REST 请求封装
const BASE = "/api"

export interface Provider {
  id: string
  name: string
  slug: string
  base_url: string
  logo_url: string | null
}

export interface KeyItem {
  id: string
  provider_name: string
  alias: string
  key_suffix: string
  status: string
  tags: string
  usage: string
  test_result: string
  created_at: string
  updated_at: string | null
}

export interface KeyCreatePayload {
  provider_id: string
  key_value: string
  alias: string
}

export interface TestResultPayload {
  ok: boolean
  latency_ms: number
  error: string | null
  models: string[]
}

/** 获取令牌 —— 从 localStorage 中读取 */
function getToken(): string {
  return localStorage.getItem("token") || ""
}

/** 封装带 Bearer 的 fetch */
async function authFetch<T>(url: string, options: RequestInit = {}): Promise<T> {
  const res = await fetch(`${BASE}${url}`, {
    ...options,
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${getToken()}`,
      ...options.headers,
    },
  })
  if (!res.ok) {
    const err = await res.json().catch(() => ({ detail: res.statusText }))
    throw new Error(err.detail || `HTTP ${res.status}`)
  }
  return res.json()
}

/** 获取厂商列表 */
export function fetchProviders(): Promise<Provider[]> {
  return authFetch("/api/providers")
}

/** 获取 Key 列表 */
export function fetchKeys(): Promise<KeyItem[]> {
  return authFetch("/api/keys")
}

/** 创建新 Key */
export function createKeyAPI(data: KeyCreatePayload): Promise<KeyItem> {
  return authFetch("/api/keys", {
    method: "POST",
    body: JSON.stringify(data),
  })
}

/** 删除 Key（软删除） */
export function deleteKeyAPI(id: string): Promise<{ ok: boolean }> {
  return authFetch(`/api/keys/${id}`, { method: "DELETE" })
}

/** 切换 Key 启用/禁用 */
export function toggleKeyAPI(id: string): Promise<{ ok: boolean; status: string }> {
  return authFetch(`/api/keys/${id}/toggle`, { method: "PUT" })
}

/** 测试 Key 连通性 */
export function testKeyAPI(id: string): Promise<TestResultPayload> {
  return authFetch(`/api/keys/${id}/test`, { method: "POST" })
}
```

- [ ] **Step 2: 创建 `client/src/stores/keyStore.ts`**

```bash
mkdir -p client/src/stores
```

```typescript
// client/src/stores/keyStore.ts
// Pinia store —— API Key 状态管理（列表、增删改、测试）
import { defineStore } from "pinia"
import { ref } from "vue"
import {
  fetchProviders,
  fetchKeys,
  createKeyAPI,
  deleteKeyAPI,
  toggleKeyAPI,
  testKeyAPI,
  type Provider,
  type KeyItem,
  type KeyCreatePayload,
  type TestResultPayload,
} from "@/api/keys"

export const useKeyStore = defineStore("keyStore", () => {
  // ── 状态 ──
  const providers = ref<Provider[]>([])
  const keys = ref<KeyItem[]>([])
  const loading = ref(false)
  const showAddDialog = ref(false)

  // ── 获取厂商列表 ──
  async function loadProviders() {
    providers.value = await fetchProviders()
  }

  // ── 获取 Key 列表 ──
  async function loadKeys() {
    loading.value = true
    try {
      keys.value = await fetchKeys()
    } finally {
      loading.value = false
    }
  }

  // ── 创建 Key ──
  async function createKey(data: KeyCreatePayload): Promise<KeyItem> {
    const created = await createKeyAPI(data)
    keys.value.unshift(created) // 插入列表头部
    return created
  }

  // ── 删除 Key ──
  async function deleteKey(id: string) {
    await deleteKeyAPI(id)
    keys.value = keys.value.filter(k => k.id !== id)
  }

  // ── 切换 Key 启用/禁用 ──
  async function toggleKey(id: string) {
    const res = await toggleKeyAPI(id)
    const key = keys.value.find(k => k.id === id)
    if (key) key.status = res.status
  }

  // ── 测试 Key 连通性 ──
  async function testKey(id: string): Promise<TestResultPayload> {
    const result = await testKeyAPI(id)
    // 测试后刷新列表以拿到最新的 test_result
    await loadKeys()
    return result
  }

  return {
    providers,
    keys,
    loading,
    showAddDialog,
    loadProviders,
    loadKeys,
    createKey,
    deleteKey,
    toggleKey,
    testKey,
  }
})
```

- [ ] **Step 3: 对 TypeScript 做语法检查**

```bash
cd client
npx vue-tsc --noEmit --project tsconfig.json 2>&1 | head -30
```

- [ ] **Step 4: 提交**

```bash
git add client/src/api/keys.ts client/src/stores/keyStore.ts
git commit -m "feat: keyStore Pinia store + API 请求封装"
```

---

### Task 6: 创建 KeysView 主页面

**Files:**
- Create: `client/src/views/KeysView.vue`

- [ ] **Step 1: 创建 `KeysView.vue`**

```bash
mkdir -p client/src/views
```

```vue
<!-- client/src/views/KeysView.vue -->
<!-- API Key 管理主页面 —— 玻璃卡片列表 + 添加按钮 -->
<template>
  <div class="keys-page">
    <!-- 顶部栏 -->
    <div class="keys-topbar">
      <h1 class="keys-title">API Key 管理</h1>
      <button class="btn-primary" @click="store.showAddDialog = true">
        + 添加新 API Key
      </button>
    </div>

    <!-- 加载中 -->
    <div v-if="store.loading" class="keys-loading">
      <p>加载中...</p>
    </div>

    <!-- Key 列表 -->
    <div v-else-if="store.keys.length > 0" class="keys-list">
      <div
        v-for="key in store.keys"
        :key="key.id"
        class="glass-card key-card"
      >
        <!-- 左侧：厂商名 + 别名 -->
        <div class="key-info">
          <h3 class="key-provider">{{ key.provider_name }}</h3>
          <p v-if="key.alias" class="key-alias">{{ key.alias }}</p>
          <!-- 后 6 位脱敏显示 -->
          <code class="key-suffix">****{{ key.key_suffix }}</code>
        </div>

        <!-- 右侧：状态 + 操作按钮 -->
        <div class="key-actions">
          <!-- 状态徽章 -->
          <span
            class="status-badge"
            :class="{ 'status-active': key.status === 'active', 'status-disabled': key.status === 'disabled' }"
          >
            {{ key.status === 'active' ? '活跃' : '已禁用' }}
          </span>

          <!-- 测试按钮 -->
          <button
            class="btn-test"
            :disabled="testingId === key.id"
            @click="handleTest(key.id)"
          >
            {{ testingId === key.id ? '测试中...' : '测试' }}
          </button>

          <!-- 启用/禁用切换 -->
          <button class="btn-toggle" @click="store.toggleKey(key.id)">
            {{ key.status === 'active' ? '禁用' : '启用' }}
          </button>

          <!-- 删除按钮 -->
          <button class="btn-danger-sm" @click="handleDelete(key.id)">
            删除
          </button>
        </div>
      </div>
    </div>

    <!-- 空状态 -->
    <div v-else class="keys-empty">
      <p class="empty-icon">🔑</p>
      <p class="empty-text">还没有添加任何 API Key</p>
      <p class="empty-hint">点击上方按钮添加你的第一个 API Key</p>
    </div>

    <!-- 添加 Key 弹窗 -->
    <AddKeyDialog v-if="store.showAddDialog" @close="store.showAddDialog = false" />

    <!-- 测试结果弹窗 -->
    <div v-if="testResult" class="modal-overlay" @click.self="testResult = null">
      <div class="glass-card test-result-modal">
        <h2>连通性测试</h2>
        <p :class="testResult.ok ? 'test-ok' : 'test-fail'">
          {{ testResult.ok ? '连接成功' : '连接失败' }}
        </p>
        <p v-if="testResult.latency_ms > 0">延迟：{{ testResult.latency_ms }}ms</p>
        <p v-if="testResult.error" class="test-error">{{ testResult.error }}</p>
        <div v-if="testResult.models.length > 0" class="test-models">
          <p>可用模型（{{ testResult.models.length }}）：</p>
          <ul>
            <li v-for="m in testResult.models.slice(0, 10)" :key="m">{{ m }}</li>
          </ul>
        </div>
        <button class="btn-primary" @click="testResult = null">关闭</button>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
// 组件逻辑 —— 页面初始化加载数据，处理测试和删除操作
import { ref, onMounted } from "vue"
import { useKeyStore } from "@/stores/keyStore"
import AddKeyDialog from "@/components/AddKeyDialog.vue"
import type { TestResultPayload } from "@/api/keys"

const store = useKeyStore()
const testingId = ref<string | null>(null)   // 当前正在测试的 Key ID
const testResult = ref<TestResultPayload | null>(null)  // 测试结果弹窗数据

// 页面挂载时加载数据
onMounted(async () => {
  await Promise.all([store.loadProviders(), store.loadKeys()])
})

// 测试连通性
async function handleTest(id: string) {
  testingId.value = id
  try {
    testResult.value = await store.testKey(id)
  } finally {
    testingId.value = null
  }
}

// 删除 Key（简单确认）
function handleDelete(id: string) {
  if (confirm("确定删除这个 API Key 吗？")) {
    store.deleteKey(id)
  }
}
</script>

<style scoped>
/* KeysView 页面样式 */
.keys-page {
  max-width: 900px;
  margin: 0 auto;
  padding: 32px 24px;
}

.keys-topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32px;
}

.keys-title {
  font-size: 28px;
  font-weight: 700;
  color: var(--text-primary);
}

.keys-loading,
.keys-empty {
  text-align: center;
  padding: 80px 0;
  color: var(--text-secondary);
}

.empty-icon {
  font-size: 48px;
  margin-bottom: 16px;
}

.empty-text {
  font-size: 18px;
  margin-bottom: 8px;
}

.empty-hint {
  font-size: 14px;
  color: var(--text-secondary);
}

.keys-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.key-card {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 24px;
}

.key-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.key-provider {
  font-size: 18px;
  font-weight: 600;
  color: var(--accent);
}

.key-alias {
  font-size: 14px;
  color: var(--text-secondary);
}

.key-suffix {
  font-size: 13px;
  color: var(--text-secondary);
  background: rgba(0, 229, 216, 0.06);
  padding: 2px 8px;
  border-radius: 6px;
  font-family: monospace;
}

.key-actions {
  display: flex;
  align-items: center;
  gap: 12px;
}

.status-badge {
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 12px;
  font-weight: 600;
  border: 1px solid;
}

.status-active {
  color: var(--success);
  border-color: var(--success);
  background: rgba(52, 211, 153, 0.08);
}

.status-disabled {
  color: var(--warning);
  border-color: var(--warning);
  background: rgba(251, 146, 60, 0.08);
}

.btn-test,
.btn-toggle {
  padding: 6px 14px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  border: 1px solid var(--border-glow);
  background: transparent;
  color: var(--text-secondary);
  transition: all 0.2s;
}

.btn-test:hover,
.btn-toggle:hover {
  border-color: var(--accent);
  color: var(--accent);
}

.btn-test:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.btn-danger-sm {
  padding: 6px 14px;
  border-radius: 8px;
  font-size: 13px;
  cursor: pointer;
  border: 1px solid rgba(239, 68, 68, 0.3);
  background: transparent;
  color: #ef4444;
  transition: all 0.2s;
}

.btn-danger-sm:hover {
  background: rgba(239, 68, 68, 0.1);
  border-color: #ef4444;
}

/* 测试结果弹窗 */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
}

.test-result-modal {
  padding: 32px;
  min-width: 360px;
  max-width: 480px;
  text-align: center;
}

.test-result-modal h2 {
  margin-bottom: 16px;
  font-size: 20px;
}

.test-ok {
  color: var(--success);
  font-size: 16px;
}

.test-fail {
  color: #ef4444;
  font-size: 16px;
}

.test-error {
  color: var(--text-secondary);
  font-size: 13px;
  margin-top: 8px;
}

.test-models {
  text-align: left;
  margin-top: 12px;
  font-size: 13px;
  color: var(--text-secondary);
}

.test-models ul {
  max-height: 160px;
  overflow-y: auto;
  padding-left: 20px;
  margin-top: 4px;
}

.test-result-modal .btn-primary {
  margin-top: 20px;
  width: 100%;
}
</style>
```

- [ ] **Step 2: 更新 router.ts 注册 KeysView 路由**

在 `client/src/router.ts` 中，将 `/keys` 路由从 `Placeholder` 替换为：

```typescript
{ path: '/keys', name: 'keys', component: () => import('@/views/KeysView.vue') },
```

- [ ] **Step 3: 提交**

```bash
git add client/src/views/KeysView.vue client/src/router.ts
git commit -m "feat: KeysView 主页面 —— 玻璃卡片列表 + 测试弹窗 + 空状态"
```

---

### Task 7: 创建 AddKeyDialog 组件

**Files:**
- Create: `client/src/components/AddKeyDialog.vue`

- [ ] **Step 1: 创建 `AddKeyDialog.vue`**

```bash
mkdir -p client/src/components
```

```vue
<!-- client/src/components/AddKeyDialog.vue -->
<!-- 添加 API Key 的玻璃模态弹窗 -->
<template>
  <div class="modal-overlay" @click.self="$emit('close')" @keydown.escape="$emit('close')">
    <div class="glass-card dialog-card">
      <h2 class="dialog-title">添加新 API Key</h2>

      <!-- 选择厂商 -->
      <label class="form-label">厂商</label>
      <select v-model="form.provider_id" class="form-select">
        <option value="" disabled>-- 选择一个 AI 厂商 --</option>
        <option v-for="p in store.providers" :key="p.id" :value="p.id">
          {{ p.name }}
        </option>
      </select>

      <!-- Key 值输入 -->
      <label class="form-label">API Key</label>
      <input
        v-model="form.key_value"
        type="password"
        class="form-input"
        placeholder="sk-..."
        @focus="showKeyInput = true"
      />
      <span v-if="showKeyInput" class="form-hint">Key 将用 AES-256-GCM 加密存储，仅后 6 位可见</span>

      <!-- 别名输入 -->
      <label class="form-label">别名（可选）</label>
      <input
        v-model="form.alias"
        type="text"
        class="form-input"
        placeholder="例如：公司账号、个人开发"
      />

      <!-- 按钮区 -->
      <div class="dialog-actions">
        <button class="btn-secondary" @click="$emit('close')">取消</button>
        <button
          class="btn-primary"
          :disabled="!canSave || saving"
          @click="handleSave"
        >
          {{ saving ? '保存中...' : '保存' }}
        </button>
      </div>

      <!-- 测试结果区域（保存前可测试） -->
      <div v-if="testResult" class="test-result-pre">
        <p :class="testResult.ok ? 'test-ok' : 'test-fail'">
          {{ testResult.ok ? '连通性正常' : '连通性异常' }}
        </p>
        <p v-if="testResult.latency_ms > 0">延迟 {{ testResult.latency_ms }}ms</p>
        <p v-if="testResult.error" class="test-err-msg">{{ testResult.error }}</p>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
// 弹窗组件逻辑 —— 收集表单数据，调用 store 创建 Key
import { reactive, ref, computed } from "vue"
import { useKeyStore } from "@/stores/keyStore"
import type { TestResultPayload } from "@/api/keys"

const emit = defineEmits<{ close: [] }>()
const store = useKeyStore()

// ── 表单状态 ──
const form = reactive({
  provider_id: "",
  key_value: "",
  alias: "",
})
const saving = ref(false)
const showKeyInput = ref(false)
const testResult = ref<TestResultPayload | null>(null)

// ── 是否可保存 ──
const canSave = computed(() => {
  return form.provider_id && form.key_value.length >= 8
})

// ── 保存 ──
async function handleSave() {
  if (!canSave.value) return
  saving.value = true
  try {
    await store.createKey({
      provider_id: form.provider_id,
      key_value: form.key_value,
      alias: form.alias,
    })
    emit("close")
  } catch (e: any) {
    alert("保存失败：" + (e.message || "未知错误"))
  } finally {
    saving.value = false
  }
}
</script>

<style scoped>
/* AddKeyDialog 样式 */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
}

.dialog-card {
  padding: 32px;
  width: 420px;
  max-height: 90vh;
  overflow-y: auto;
}

.dialog-title {
  font-size: 22px;
  font-weight: 700;
  margin-bottom: 24px;
  color: var(--accent);
}

.form-label {
  display: block;
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 6px;
  margin-top: 16px;
}

.form-select,
.form-input {
  width: 100%;
  padding: 10px 14px;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid var(--border-glow);
  border-radius: 10px;
  color: var(--text-primary);
  font-size: 14px;
  outline: none;
  transition: border-color 0.2s;
}

.form-select:focus,
.form-input:focus {
  border-color: var(--accent);
}

.form-hint {
  display: block;
  margin-top: 4px;
  font-size: 12px;
  color: var(--text-secondary);
  opacity: 0.7;
}

.dialog-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  margin-top: 24px;
}

.btn-secondary {
  padding: 10px 20px;
  border-radius: 12px;
  font-size: 14px;
  cursor: pointer;
  border: 1px solid var(--border-glow);
  background: transparent;
  color: var(--text-secondary);
  transition: border-color 0.2s;
}

.btn-secondary:hover {
  border-color: var(--text-secondary);
}

.btn-primary:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.test-result-pre {
  margin-top: 16px;
  padding: 12px;
  border-radius: 10px;
  background: rgba(0, 0, 0, 0.15);
  text-align: center;
  font-size: 13px;
}

.test-ok {
  color: var(--success);
}

.test-fail {
  color: #ef4444;
}

.test-err-msg {
  color: var(--text-secondary);
  margin-top: 4px;
}
</style>
```

- [ ] **Step 2: 在 KeysView 中集成 AddKeyDialog**

`KeysView.vue` 中已在使用 `<AddKeyDialog>` 组件（见 Task 6 Step 1），确认 `import` 路径正确：

```typescript
import AddKeyDialog from "@/components/AddKeyDialog.vue"
```

- [ ] **Step 3: 对 TypeScript 做语法检查**

```bash
cd client
npx vue-tsc --noEmit --project tsconfig.json 2>&1 | head -30
```

- [ ] **Step 4: 提交**

```bash
git add client/src/components/AddKeyDialog.vue
git commit -m "feat: AddKeyDialog 玻璃模态弹窗 —— 厂商选择 + Key 输入 + 保存"
```

---

### Task 8: 端到端验证并提交

- [ ] **Step 1: 启动后端**

```bash
cd server
source venv/bin/activate
uvicorn main:app --reload --port 8000 &
```

- [ ] **Step 2: 启动前端**

```bash
cd client
npm run dev &
```

- [ ] **Step 3: 手动测试清单**

| 测试项 | 操作 | 预期 |
|--------|------|------|
| 认证后进入 /keys | 登录后访问 `/#/keys` | 看到 "API Key 管理" 标题 + "+ 添加" 按钮 |
| 空状态 | 无 Key 时 | 显示空状态提示 |
| 打开添加弹窗 | 点击 "+ 添加新 API Key" | 玻璃模态弹窗弹出 |
| 选择厂商 | 下拉框选 OpenAI | 显示 7 家厂商 |
| 添加 Key | 填写表单，点保存 | 弹窗关闭，列表出现新卡片 |
| 脱敏显示 | 查看卡片 | 显示 `****xxxxxx`（后 6 位） |
| 测试连通性 | 点击测试按钮 | 弹窗显示结果（ok/fail + 延迟） |
| 切换状态 | 点击启用/禁用 | 状态徽章变色 |
| 删除确认 | 点击删除 → 确认 | Key 从列表消失 |
| 关闭弹窗 | 按 ESC 或点遮罩 | 弹窗关闭 |

- [ ] **Step 4: 提交（如有修改）**

```bash
git status
# 如有修改，追加提交
git add -A
git commit -m "chore: 阶段 3 端到端验证完成"
```

---

## 阶段 3 完成检查清单

- [ ] `GET /api/providers` 返回 7 家厂商
- [ ] `POST /api/keys` 创建 Key，AES-256-GCM 加密存入数据库
- [ ] `GET /api/keys` 列表不包含 `encrypted_key`，仅返回 `key_suffix`（后 6 位）
- [ ] `DELETE /api/keys/{id}` 软删除（status='deleted'）
- [ ] `PUT /api/keys/{id}/toggle` 在 active/disabled 间切换
- [ ] `POST /api/keys/{id}/test` 调厂商 /models 接口，记录结果到 test_result 字段
- [ ] 前端 `/` 路由显示 KeysView 玻璃卡片列表
- [ ] "添加" 按钮弹出 AddKeyDialog 模态窗，可选择厂商、输入 Key、填写别名
- [ ] Key 卡片显示厂商名、状态徽章、脱敏后缀、操作按钮
- [ ] 加密解密往返验证：`decrypt(encrypt(plaintext)) == plaintext`
- [ ] 前端永远拿不到完整 Key（API 不返回 `encrypted_key`）
