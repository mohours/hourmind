# HourMind 阶段 2 — 认证系统 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现完整的密码认证流程 —— 首次设置密码、登录、Token 验证、路由保护。

**Architecture:** `server/auth.py` 提供 JWT 创建/验证 + FastAPI 依赖注入；`server/models.py` 定义 Pydantic 请求/响应模型；`server/routes/auth.py` 提供 3 个 REST 端点；前端 `appStore` 管理认证状态，`SetupView` / `LoginView` 实现玻璃态密码表单。

**Tech Stack:** Python 3.12, FastAPI, python-jose, bcrypt / Vue 3, TypeScript, Pinia, Vue Router

---

### Task 1: JWT 工具模块 + Pydantic 模型

**Files:**
- Create: `server/auth.py`
- Create: `server/models.py`

- [ ] **Step 1: 创建 auth.py —— JWT 创建、验证、依赖注入**

```python
# server/auth.py
# JWT Token 创建/验证 + FastAPI 认证依赖注入
import time
from jose import jwt, JWTError, ExpiredSignatureError
from fastapi import Header, HTTPException
from config import JWT_SECRET

TOKEN_EXPIRE_HOURS = 24  # Token 有效期 24 小时


def create_token(user_id: str = "default") -> str:
    """创建 JWT Token —— 包含 user_id 和 24h 过期时间"""
    now = int(time.time())
    payload = {
        "user_id": user_id,
        "iat": now,                       # 签发时间
        "exp": now + TOKEN_EXPIRE_HOURS * 3600,  # 过期时间
    }
    return jwt.encode(payload, JWT_SECRET, algorithm="HS256")


def verify_token(token: str) -> dict:
    """验证 JWT Token —— 返回 payload，无效/过期则抛出"""
    try:
        payload = jwt.decode(token, JWT_SECRET, algorithms=["HS256"])
        return payload
    except ExpiredSignatureError:
        raise HTTPException(status_code=401, detail="Token 已过期")
    except JWTError:
        raise HTTPException(status_code=401, detail="Token 无效")


def require_auth(authorization: str = Header(None)) -> dict:
    """FastAPI 依赖注入 —— 从 Bearer Header 提取并验证 Token"""
    if not authorization or not authorization.startswith("Bearer "):
        raise HTTPException(status_code=401, detail="未认证")
    token = authorization.replace("Bearer ", "")
    return verify_token(token)
```

- [ ] **Step 2: 创建 models.py —— 认证相关 Pydantic 模型**

```python
# server/models.py
# Pydantic 数据模型 —— 请求/响应 Schema
from pydantic import BaseModel


class AuthRequest(BaseModel):
    """认证请求 —— 密码"""
    password: str


class AuthResponse(BaseModel):
    """认证响应 —— JWT Token"""
    token: str


class CheckResponse(BaseModel):
    """Token 检查响应"""
    valid: bool
    setup_required: bool = False
```

- [ ] **Step 3: 提交**

```bash
git add server/auth.py server/models.py
git commit -m "feat: JWT 工具模块 + 认证 Pydantic 模型"
```

---

### Task 2: 认证路由 — 3 个 REST 端点

**Files:**
- Create: `server/routes/__init__.py`
- Create: `server/routes/auth.py`

- [ ] **Step 1: 创建 routes 目录和 __init__.py**

```bash
mkdir -p server/routes
```

```python
# server/routes/__init__.py
# 路由模块初始化
```

- [ ] **Step 2: 创建 routes/auth.py —— setup / login / check**

```python
# server/routes/auth.py
# 认证路由 —— 密码设置、登录、Token 验证
import bcrypt
import uuid
from fastapi import APIRouter, Depends
from database import get_db
from auth import create_token, verify_token, require_auth
from models import AuthRequest, AuthResponse, CheckResponse

router = APIRouter(prefix="/api/auth", tags=["auth"])


@router.post("/setup", response_model=AuthResponse)
def setup(body: AuthRequest):
    """首次设置密码 —— 仅在 config 表中无 password_hash 时可用"""
    db = get_db()
    try:
        existing = db.execute(
            "SELECT value FROM config WHERE key = ?", ("password_hash",)
        ).fetchone()
        if existing:
            from fastapi import HTTPException
            raise HTTPException(status_code=400, detail="密码已设置，请使用登录接口")

        # bcrypt 哈希密码（cost=12）
        hashed = bcrypt.hashpw(body.password.encode(), bcrypt.gensalt(rounds=12))
        hashed_str = hashed.decode()

        # 存入 config 表
        db.execute(
            "INSERT INTO config (key, value) VALUES (?, ?)",
            ("password_hash", hashed_str),
        )
        db.commit()

        # 签发 Token
        token = create_token()
        return AuthResponse(token=token)
    finally:
        db.close()


@router.post("/login", response_model=AuthResponse)
def login(body: AuthRequest):
    """密码登录 —— 验证密码后返回 JWT"""
    db = get_db()
    try:
        row = db.execute(
            "SELECT value FROM config WHERE key = ?", ("password_hash",)
        ).fetchone()
        if not row:
            from fastapi import HTTPException
            raise HTTPException(status_code=400, detail="尚未设置密码，请先完成初始化")

        hashed = row["value"]
        # bcrypt 验证
        if not bcrypt.checkpw(body.password.encode(), hashed.encode()):
            from fastapi import HTTPException
            raise HTTPException(status_code=401, detail="密码错误")

        token = create_token()
        return AuthResponse(token=token)
    finally:
        db.close()


@router.get("/check", response_model=CheckResponse)
def check(payload: dict = Depends(require_auth)):
    """验证当前 Token 是否有效 —— 并检查是否需要初始化"""
    db = get_db()
    try:
        row = db.execute(
            "SELECT value FROM config WHERE key = ?", ("password_hash",)
        ).fetchone()
        setup_required = row is None
        return CheckResponse(valid=True, setup_required=setup_required)
    finally:
        db.close()
```

- [ ] **Step 3: 用 curl 验证三个端点**

```bash
# 启动后端
cd server && source venv/bin/activate && python main.py &

# 1. 首次设置密码
curl -X POST http://localhost:8000/api/auth/setup \
  -H 'Content-Type: application/json' \
  -d '{"password":"test123"}'
# 预期返回: {"token":"eyJ..."}

# 2. 重复 setup 应失败
curl -X POST http://localhost:8000/api/auth/setup \
  -H 'Content-Type: application/json' \
  -d '{"password":"test123"}'
# 预期返回: {"detail":"密码已设置，请使用登录接口"}

# 3. 登录
TOKEN=$(curl -s -X POST http://localhost:8000/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"password":"test123"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])")

# 4. 验证 Token
curl -s http://localhost:8000/api/auth/check \
  -H "Authorization: Bearer $TOKEN"
# 预期返回: {"valid":true,"setup_required":false}

# 5. 无 Token 访问应失败
curl -s http://localhost:8000/api/auth/check
# 预期返回: {"detail":"未认证"}
```

- [ ] **Step 4: 提交**

```bash
git add server/routes/
git commit -m "feat: 认证 REST 端点 —— setup / login / check"
```

---

### Task 3: 注册路由到 main.py

**Files:**
- Modify: `server/main.py`

- [ ] **Step 1: 在 main.py 中导入并注册认证路由**

在 `server/main.py` 的 `app = FastAPI(...)` 行之后，`if __name__ == "__main__"` 之前添加：

```python
# 注册认证路由
from routes.auth import router as auth_router
app.include_router(auth_router)
```

- [ ] **Step 2: 验证路由已注册**

```bash
cd server && source venv/bin/activate && python -c "
from main import app
routes = [r.path for r in app.routes]
print('Routes:', routes)
assert '/api/auth/setup' in routes
assert '/api/auth/login' in routes
assert '/api/auth/check' in routes
print('All routes registered successfully')
"
```

- [ ] **Step 3: 提交**

```bash
git add server/main.py
git commit -m "feat: 注册认证路由到 FastAPI 应用"
```

---

### Task 4: 前端 API 工具 + 认证 Store

**Files:**
- Create: `client/src/api/index.ts`
- Create: `client/src/stores/appStore.ts`

- [ ] **Step 1: 创建 api/index.ts —— 简单 HTTP 封装**

```typescript
// client/src/api/index.ts
// 前端 API 封装 —— 统一请求方法，通过 Vite 代理转发到后端 8000
const BASE = 'http://localhost:5173/api'  // Vite 代理到 FastAPI 8000

export async function api(method: string, path: string, body?: any, token?: string) {
  const headers: Record<string, string> = { 'Content-Type': 'application/json' }
  if (token) headers['Authorization'] = `Bearer ${token}`
  const res = await fetch(BASE + path, {
    method,
    headers,
    body: body ? JSON.stringify(body) : undefined,
  })
  return res.json()
}
```

- [ ] **Step 2: 创建 stores/appStore.ts —— 认证状态管理**

```typescript
// client/src/stores/appStore.ts
// 认证状态管理 —— token 持久化、登录/登出/检查
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { api } from '@/api'

export const useAppStore = defineStore('app', () => {
  const token = ref(localStorage.getItem('hourmind_token') || '')
  const isAuthenticated = ref(false)
  const isSetupRequired = ref(false)

  /** 检查当前 token 是否有效 */
  async function checkAuth() {
    if (!token.value) {
      isAuthenticated.value = false
      // 发一个不带 token 的 check 请求，看看是否需要 setup
      try {
        const res = await fetch('http://localhost:5173/api/auth/check')
        const data = await res.json()
        isSetupRequired.value = !!data.setup_required
      } catch {
        isSetupRequired.value = false
      }
      return
    }
    try {
      const data = await api('GET', '/auth/check', undefined, token.value)
      isAuthenticated.value = data.valid
      isSetupRequired.value = data.setup_required
    } catch {
      // Token 无效，清除
      isAuthenticated.value = false
      token.value = ''
      localStorage.removeItem('hourmind_token')
    }
  }

  /** 首次设置密码 */
  async function setup(password: string) {
    const data = await api('POST', '/auth/setup', { password })
    if (data.token) {
      token.value = data.token
      localStorage.setItem('hourmind_token', data.token)
      isAuthenticated.value = true
      isSetupRequired.value = false
    }
    return data
  }

  /** 登录 */
  async function login(password: string) {
    const data = await api('POST', '/auth/login', { password })
    if (data.token) {
      token.value = data.token
      localStorage.setItem('hourmind_token', data.token)
      isAuthenticated.value = true
      isSetupRequired.value = false
    }
    return data
  }

  /** 登出 */
  function logout() {
    token.value = ''
    isAuthenticated.value = false
    localStorage.removeItem('hourmind_token')
    window.location.reload()
  }

  return { token, isAuthenticated, isSetupRequired, checkAuth, setup, login, logout }
})
```

**注意:** `client/src/stores/` 目录在阶段 1 应该是空的 — 确认目录存在，没有则创建：

```bash
mkdir -p client/src/stores
```

- [ ] **Step 3: 提交**

```bash
git add client/src/api/ client/src/stores/
git commit -m "feat: 前端 API 工具 + Pinia 认证 Store"
```

---

### Task 5: 实现密码设置页面 SetupView

**Files:**
- Modify: `client/src/views/SetupView.vue`

- [ ] **Step 1: 重写 SetupView.vue —— 密码设置表单**

```vue
<!-- client/src/views/SetupView.vue -->
<!-- 首次设置密码页面 —— Quantum Glass 风格 -->
<template>
  <div class="auth-container">
    <!-- 标题 -->
    <h1 class="auth-title">HourMind</h1>
    <p class="auth-subtitle">小时智脑 · 首次设置</p>

    <!-- 玻璃态表单卡片 -->
    <div class="glass-card" style="width: 400px; padding: 40px 36px">
      <h2 style="margin-bottom: 8px; font-size: 20px">设置主密码</h2>
      <p style="color: var(--text-secondary); font-size: 14px; margin-bottom: 24px">
        这是您访问 HourMind 的唯一凭证，请妥善保管
      </p>

      <input
        v-model="password"
        type="password"
        placeholder="输入密码（至少 6 位）"
        class="auth-input"
        @keyup.enter="handleSetup"
      />
      <input
        v-model="confirmPassword"
        type="password"
        placeholder="确认密码"
        class="auth-input"
        @keyup.enter="handleSetup"
      />

      <p v-if="error" class="error-text">{{ error }}</p>

      <button
        class="btn-primary"
        style="width: 100%; margin-top: 8px"
        :disabled="loading"
        @click="handleSetup"
      >
        {{ loading ? '设置中...' : '开始使用' }}
      </button>
    </div>
  </div>
</template>

<script setup lang="ts">
// SetupView 逻辑 —— 验证密码一致性，调用 appStore.setup()
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { useAppStore } from '@/stores/appStore'

const router = useRouter()
const appStore = useAppStore()

const password = ref('')
const confirmPassword = ref('')
const error = ref('')
const loading = ref(false)

async function handleSetup() {
  error.value = ''

  if (password.value.length < 6) {
    error.value = '密码至少 6 位'
    return
  }
  if (password.value !== confirmPassword.value) {
    error.value = '两次输入的密码不一致'
    return
  }

  loading.value = true
  try {
    const res = await appStore.setup(password.value)
    if (res.token) {
      router.replace('/')
    } else if (res.detail) {
      error.value = res.detail
    }
  } catch (e: any) {
    error.value = '网络错误，请检查后端是否启动'
  } finally {
    loading.value = false
  }
}
</script>
```

- [ ] **Step 2: 添加认证表单专用样式**

在 `client/src/style.css` 末尾追加：

```css
/* 认证表单输入框 */
.auth-input {
  width: 100%;
  padding: 12px 16px;
  margin-bottom: 12px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid var(--border-glow);
  border-radius: 12px;
  color: var(--text-primary);
  font-size: 15px;
  outline: none;
  transition: border-color 0.2s;
}

.auth-input::placeholder {
  color: var(--text-secondary);
}

.auth-input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 2px rgba(0, 229, 216, 0.15);
}

.auth-title {
  font-size: 48px;
  background: linear-gradient(135deg, var(--accent), var(--accent-purple));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  letter-spacing: 2px;
}

.auth-subtitle {
  color: var(--text-secondary);
  font-size: 16px;
  margin-bottom: 8px;
}

.error-text {
  color: #EF4444;
  font-size: 13px;
  margin: 4px 0;
}
```

- [ ] **Step 3: 验证页面**

```bash
cd client && npm run dev
```

访问 `http://localhost:5173/#/setup`，应看到玻璃态密码设置表单。  
输入密码 + 确认密码，点击"开始使用"应调用 `/api/auth/setup`。

- [ ] **Step 4: 提交**

```bash
git add client/src/views/SetupView.vue client/src/style.css
git commit -m "feat: 密码设置页面 —— 玻璃态 SetupView"
```

---

### Task 6: 实现登录页面 LoginView

**Files:**
- Modify: `client/src/views/LoginView.vue`

- [ ] **Step 1: 重写 LoginView.vue —— 登录表单**

```vue
<!-- client/src/views/LoginView.vue -->
<!-- 登录页面 —— Quantum Glass 风格 -->
<template>
  <div class="auth-container">
    <!-- 标题 -->
    <h1 class="auth-title">HourMind</h1>
    <p class="auth-subtitle">小时智脑</p>

    <!-- 玻璃态表单卡片 -->
    <div class="glass-card" style="width: 400px; padding: 40px 36px">
      <h2 style="margin-bottom: 24px; font-size: 20px">登录</h2>

      <input
        v-model="password"
        type="password"
        placeholder="请输入主密码"
        class="auth-input"
        @keyup.enter="handleLogin"
      />

      <p v-if="error" class="error-text">{{ error }}</p>

      <button
        class="btn-primary"
        style="width: 100%; margin-top: 8px"
        :disabled="loading"
        @click="handleLogin"
      >
        {{ loading ? '验证中...' : '进入' }}
      </button>
    </div>
  </div>
</template>

<script setup lang="ts">
// LoginView 逻辑 —— 调用 appStore.login()，成功后跳转到首页
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { useAppStore } from '@/stores/appStore'

const router = useRouter()
const appStore = useAppStore()

const password = ref('')
const error = ref('')
const loading = ref(false)

async function handleLogin() {
  error.value = ''
  if (!password.value) {
    error.value = '请输入密码'
    return
  }

  loading.value = true
  try {
    const res = await appStore.login(password.value)
    if (res.token) {
      router.replace('/')
    } else if (res.detail) {
      error.value = res.detail
    }
  } catch (e: any) {
    error.value = '网络错误，请检查后端是否启动'
  } finally {
    loading.value = false
  }
}
</script>
```

- [ ] **Step 2: 验证登录流程**

```bash
# 先通过 setup 设置密码（如在上一步已设置则跳过）
# 然后访问 http://localhost:5173/#/login
# 输入密码点"进入"，应跳转到首页
```

- [ ] **Step 3: 提交**

```bash
git add client/src/views/LoginView.vue
git commit -m "feat: 登录页面 —— 玻璃态 LoginView"
```

---

### Task 7: 更新 App.vue — 认证状态路由分发

**Files:**
- Modify: `client/src/App.vue`

- [ ] **Step 1: 重写 App.vue —— 根据认证状态显示不同页面**

```vue
<!-- client/src/App.vue -->
<!-- 根组件 —— 根据认证状态分发到 Setup / Login / 主界面 -->
<template>
  <!-- 未初始化 → 显示设置密码页 -->
  <SetupView v-if="!store.isAuthenticated && store.isSetupRequired" />

  <!-- 已初始化但未登录 → 显示登录页 -->
  <LoginView v-else-if="!store.isAuthenticated" />

  <!-- 已认证 → 显示主界面路由 -->
  <router-view v-else />
</template>

<script setup lang="ts">
// App.vue 逻辑 —— mounted 时检查认证状态
import { onMounted } from 'vue'
import SetupView from '@/views/SetupView.vue'
import LoginView from '@/views/LoginView.vue'
import { useAppStore } from '@/stores/appStore'

const store = useAppStore()

onMounted(async () => {
  await store.checkAuth()
})
</script>
```

**注意:** 此时 `router.ts` 中的 `/setup` 和 `/login` 路由可以保留，但 `App.vue` 直接通过条件渲染控制页面显示，不再通过路由跳转到这两个页面。

- [ ] **Step 2: 验证完整认证流程**

```bash
# 1. 清空数据库中的密码（模拟首次使用）
cd server
sqlite3 data/hourmind.db "DELETE FROM config WHERE key='password_hash';"

# 2. 启动前后端
python main.py &
cd ../client && npm run dev &

# 3. 浏览器访问 http://localhost:5173
#    - 预期看到 SetupView（设置密码页）
#    - 设置密码后跳转到主页
#    - 刷新页面 → 自动 checkAuth → token 有效 → 显示主页
#    - 清除 localStorage 中的 token → 刷新 → 看到 LoginView
```

- [ ] **Step 3: 提交**

```bash
git add client/src/App.vue
git commit -m "feat: App.vue 认证状态路由分发"
```

---

## 阶段 2 完成检查清单

- [ ] `POST /api/auth/setup` — 首次设置密码成功，返回 JWT
- [ ] `POST /api/auth/setup` — 重复调用返回 "密码已设置" 错误
- [ ] `POST /api/auth/login` — 正确密码返回 JWT，错误密码返回 "密码错误"
- [ ] `GET /api/auth/check` — 有效 Token 返回 `{"valid":true,"setup_required":false}`
- [ ] `GET /api/auth/check` — 无 Token 返回 401
- [ ] `GET /api/auth/check` — 数据库无 password_hash 时返回 `setup_required: true`
- [ ] 前端首次访问 → 显示 SetupView → 设置密码 → 跳转主页
- [ ] 前端刷新页面 → Token 仍在 localStorage → 直接进入主页
- [ ] 前端访问 → 已设置密码但无 Token → 显示 LoginView → 登录 → 跳转主页
- [ ] 错误的密码在登录页显示错误提示
